id = "dev.dart-analysis-cache"
schema_version = 3
rule_version = 2
platform = "windows"
category = "development"
risk = "recoverable"
default_selected = false
recommended_selected = true
required_stopped_processes = ["dart", "flutter"]

[[applicability]]
kind = "anyOf"
items = [
  { kind = "executableAvailable", names = ["dart.exe", "flutter.bat"] },
  { kind = "anyRootExists" },
]

[[roots]]
template = "${local_app_data}/.dartServer"
verified_rebuildable = true

[matcher]
kind = "all"

[execution]
kind = "deleteWholeRoot"
requires_app_close = true

[verification]
lifecycle = "verified"
evidence = "The Flutter repository identifies .dartServer as cached analysis results and uses Local AppData for the Windows analyzer state location; projects, SDKs, packages, and editor configuration remain outside the cleanup root"
verified_at = "2026-08-03"
verified_platform = "windows"
references = [
  "https://github.com/flutter/flutter/blob/master/dev/devicelab/lib/tasks/analysis.dart",
  "https://github.com/flutter/flutter/blob/master/dev/snippets/test/filesystem_resource_provider.dart",
]
