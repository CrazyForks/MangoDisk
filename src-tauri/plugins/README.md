# Tauri plugins

Add a custom plugin only when a capability needs an independent permission
boundary, lifecycle, or TypeScript binding. Follow the plugin integration
workflow in the repository `AGENTS.md`.
